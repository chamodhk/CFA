- sizeof is not a function but an operator
- whats the difference?
    - function jumps to another place when it is executed
    - but this doesn't

- why pointers are 8 byte
    - it is just an address
    - I use a 64 bit laptop
    - so that means i have 64 bits for addresses

